# 实现计划：代码审计和文档整理

## 概述

本实现计划将代码审计和文档整理功能分解为可执行的编码任务。该功能包含 5 个核心组件：ESLint 迁移器、注释检查器、文档整理器、代码清理器和报告生成器。实现将采用 TypeScript，使用 Node.js 18+ 运行时，集成 ESLint 9.x、fast-check 属性测试框架和 commander 命令行工具。

## 任务

- [x] 1. 搭建项目结构和核心类型定义
  - 创建 `src/` 目录结构（audit、eslint、comments、docs、cleaner、reporter）
  - 定义核心类型接口（AuditResult、CheckResult、Issue、AuditConfig 等）
  - 配置 TypeScript 编译选项（tsconfig.json）
  - 配置 Vitest 测试框架和 fast-check
  - 安装必要依赖（@typescript-eslint/parser、vue-eslint-parser、fs-extra、commander）
  - _需求：1.1, 1.3, 1.4, 2.1, 6.1_

- [x] 2. 实现 ESLint 配置迁移器
  - [x] 2.1 实现配置解析和转换逻辑
    - 创建 `src/eslint/ESLintMigrator.ts`
    - 实现 `generateNewConfig()` 方法，将旧版配置转换为扁平配置格式
    - 实现 `convertEnv()` 方法，转换环境变量为 globals
    - 实现 `convertPlugins()` 方法，转换插件格式
    - 实现 `convertIgnorePatterns()` 方法
    - _需求：1.1, 1.3, 1.4_

  - [x] 2.2 编写 ESLint 迁移器的属性测试
    - **属性 1：配置规则保留**
    - **验证需求：1.3**
    - 使用 fast-check 生成随机配置对象
    - 验证迁移后所有规则都被保留且值相同

  - [x] 2.3 实现配置验证器
    - 创建 `src/eslint/ConfigValidator.ts`
    - 实现 `validate()` 方法，比较新旧配置的等效性
    - 实现 `compareRules()` 方法
    - 实现 `runESLint()` 方法，在测试文件上运行配置
    - _需求：8.1, 8.2, 8.3, 8.4_

  - [x] 2.4 编写配置验证器的属性测试
    - **属性 2：文件类型匹配**
    - **验证需求：1.4**
    - **属性 26：配置等效性验证**
    - **验证需求：8.2, 8.3, 8.4**
    - **属性 27：Glob 模式匹配正确性**
    - **验证需求：8.6**

  - [x] 2.5 实现迁移命令和文件操作
    - 实现 `migrate()` 方法，执行完整迁移流程
    - 实现备份机制
    - 实现旧配置文件删除逻辑
    - 添加 dry-run 模式支持
    - _需求：1.2, 7.2, 7.3_

  - [x] 2.6 编写迁移操作的单元测试
    - 测试标准迁移场景
    - 测试备份和恢复机制
    - 测试错误处理（文件不存在、权限不足）

- [x] 3. 检查点 - 确保 ESLint 迁移器测试通过
  - 运行所有 ESLint 迁移器相关测试
  - 验证配置转换的正确性
  - 如有问题请询问用户

- [x] 4. 实现注释检查器
  - [x] 4.1 实现 AST 解析和注释提取
    - 创建 `src/comments/CommentChecker.ts`
    - 实现 `parseFile()` 方法，支持 .ts、.vue、.js 文件
    - 实现 `extractComments()` 方法，提取代码注释
    - 实现 `findMissingComments()` 方法，识别缺少注释的代码结构
    - _需求：2.1, 2.2_

  - [x] 4.2 编写注释识别的属性测试
    - **属性 3：文件扫描完整性**
    - **验证需求：2.1**
    - **属性 4：注释识别准确性**
    - **验证需求：2.2**

  - [x] 4.3 实现注释质量评分器
    - 创建 `src/comments/QualityScorer.ts`
    - 实现 `calculateCoverage()` 方法，计算注释覆盖率
    - 实现 `calculateQualityScore()` 方法，计算质量评分（0-100）
    - 实现 `calculatePublicAPICoverage()` 方法
    - 实现 `calculateChineseCommentRatio()` 方法
    - _需求：9.1, 9.2, 9.3_

  - [x] 4.4 编写质量评分的属性测试
    - **属性 28：注释覆盖率计算正确性**
    - **验证需求：9.1**
    - **属性 29：公共 API 识别准确性**
    - **验证需求：9.2**
    - **属性 30：质量评分范围有效性**
    - **验证需求：9.3**

  - [x] 4.5 实现注释分析器和建议生成
    - 创建 `src/comments/CommentAnalyzer.ts`
    - 实现标识符和技术术语过滤逻辑
    - 实现 `generateSuggestions()` 方法，生成改进建议
    - 实现 `aggregateResults()` 方法，聚合整体报告
    - _需求：2.4, 2.5, 9.4, 9.5, 9.6_

  - [x] 4.6 编写注释分析器的属性测试
    - **属性 6：标识符过滤正确性**
    - **验证需求：2.4, 2.5**
    - **属性 31：整体报告聚合正确性**
    - **验证需求：9.4**
    - **属性 32：阈值过滤准确性**
    - **验证需求：9.5**
    - **属性 33：改进建议针对性**
    - **验证需求：9.6**

  - [x] 4.7 编写注释检查器的单元测试
    - 测试典型代码文件的注释检查
    - 测试边界情况（空文件、无注释文件）
    - 测试 Vue 组件的注释检查

- [x] 5. 检查点 - 确保注释检查器测试通过
  - 运行所有注释检查器相关测试
  - 验证注释识别和评分的准确性
  - 如有问题请询问用户

- [x] 6. 实现文档整理器
  - [x] 6.1 实现文档扫描和分类
    - 创建 `src/docs/DocOrganizer.ts`
    - 实现 `scanDocuments()` 方法，扫描项目中的文档文件
    - 实现 `classifyDocuments()` 方法，按类型分类文档
    - 实现 `determineCategory()` 方法，基于路径和内容判断类别
    - _需求：3.1, 3.2_

  - [x] 6.2 编写文档分类的属性测试
    - **属性 8：文档分类一致性**
    - **验证需求：3.2**
    - **属性 10：排除规则有效性**
    - **验证需求：3.4**

  - [x] 6.3 实现文档移动和路径更新
    - 创建 `src/docs/PathResolver.ts`
    - 实现文件移动逻辑，支持冲突检测
    - 实现 `updateLinks()` 方法，更新文档中的相对路径引用
    - 实现 `resolveRelativePath()` 和 `calculateRelativePath()` 方法
    - _需求：3.3, 3.6, 7.4_

  - [x] 6.4 编写文档移动的属性测试
    - **属性 9：文件移动保留内容**
    - **验证需求：3.3**
    - **属性 12：路径引用更新正确性**
    - **验证需求：3.6, 7.6**
    - **属性 24：文件冲突检测**
    - **验证需求：7.4**

  - [x] 6.5 实现文档索引生成器
    - 创建 `src/docs/IndexGenerator.ts`
    - 实现 `generateIndex()` 方法，生成 docs/README.md 索引文件
    - 实现 `generateStructureDiagram()` 方法，生成目录结构图
    - _需求：3.5, 7.7_

  - [x] 6.6 编写索引生成的属性测试
    - **属性 11：索引完整性**
    - **验证需求：3.5**
    - **属性 25：文档结构图完整性**
    - **验证需求：7.7**

  - [x] 6.7 实现文档整理主流程
    - 实现 `organize()` 方法，协调整个整理流程
    - 实现 dry-run 模式
    - 实现备份机制
    - 生成迁移报告
    - _需求：7.1, 7.2, 7.3, 3.7_

  - [x] 6.8 编写文档整理的属性测试
    - **属性 13：移动操作可追溯性**
    - **验证需求：3.7**
    - **属性 22：Dry-run 模式不修改文件系统**
    - **验证需求：7.2**
    - **属性 23：备份完整性**
    - **验证需求：7.3**

  - [x] 6.9 编写文档整理器的单元测试
    - 测试标准文档整理场景
    - 测试文档分类逻辑
    - 测试链接更新功能

- [x] 7. 检查点 - 确保文档整理器测试通过
  - 运行所有文档整理器相关测试
  - 验证文档移动和链接更新的正确性
  - 如有问题请询问用户

- [x] 8. 实现代码清理器
  - [x] 8.1 实现调试代码扫描和清理
    - 创建 `src/cleaner/CodeCleaner.ts`
    - 实现 `scanDebugCode()` 方法，扫描 console.debug 语句
    - 实现 `cleanDebugCode()` 方法，支持交互式和批量清理模式
    - 实现 `removeDebugLine()` 方法
    - _需求：4.1, 4.2, 4.3, 4.4_

  - [x] 8.2 编写调试代码清理的属性测试
    - **属性 5：报告完整性**
    - **验证需求：4.2**
    - **属性 14：选择性代码删除**
    - **验证需求：4.5**

  - [x] 8.3 实现待办标记扫描和管理
    - 创建 `src/cleaner/TodoExtractor.ts`
    - 实现 `scanTodos()` 方法，扫描 TODO 和 FIXME 标记
    - 实现 `generateTodoList()` 方法，生成 Markdown 格式清单
    - 实现待办事项删除逻辑
    - _需求：5.1, 5.2, 5.3, 5.4, 5.6_

  - [x] 8.4 编写待办标记管理的属性测试
    - **属性 15：待办事项分组逻辑**
    - **验证需求：5.3**
    - **属性 16：Markdown 格式有效性**
    - **验证需求：5.4**
    - **属性 17：待办事项删除准确性**
    - **验证需求：5.6**

  - [x] 8.5 编写代码清理器的单元测试
    - 测试调试代码扫描
    - 测试待办标记提取
    - 测试交互式清理模式

- [x] 9. 实现报告生成器
  - [x] 9.1 实现 JSON 和 HTML 报告生成
    - 创建 `src/reporter/Reporter.ts`
    - 实现 `generateJSON()` 方法
    - 实现 `generateHTML()` 方法，生成可视化报告
    - 创建 HTML 模板
    - _需求：2.6, 6.5_

  - [x] 9.2 编写报告生成的属性测试
    - **属性 7：JSON 序列化往返**
    - **验证需求：2.6**
    - **属性 20：HTML 报告格式有效性**
    - **验证需求：6.5**

  - [x] 9.3 实现 JUnit XML 报告和 CI 模式
    - 实现 `generateJUnit()` 方法，生成 JUnit XML 格式报告
    - 实现 `logCI()` 方法，输出简洁的 CI 日志
    - _需求：10.2, 10.3_

  - [x] 9.4 编写 CI 模式的属性测试
    - **属性 34：CI 模式输出简洁性**
    - **验证需求：10.2**
    - **属性 35：JUnit XML 格式有效性**
    - **验证需求：10.3**

  - [x] 9.5 编写报告生成器的单元测试
    - 测试 JSON 报告生成
    - 测试 HTML 报告生成
    - 测试 JUnit XML 报告生成

- [x] 10. 实现审计工具核心
  - [x] 10.1 实现审计工具主逻辑
    - 创建 `src/audit/AuditTool.ts`
    - 实现 `run()` 方法，协调各个检查器的执行
    - 实现检查结果聚合逻辑
    - 实现质量门槛验证
    - 实现增量检查模式
    - _需求：6.1, 6.2, 6.3, 10.4, 10.7_

  - [x] 10.2 编写审计工具的属性测试
    - **属性 18：命令行参数解析正确性**
    - **验证需求：6.3**
    - **属性 19：自动修复幂等性**
    - **验证需求：6.4**
    - **属性 21：错误退出码一致性**
    - **验证需求：6.6, 10.5**
    - **属性 36：质量门槛应用正确性**
    - **验证需求：10.4**
    - **属性 37：增量检查文件集合正确性**
    - **验证需求：10.7**

  - [x] 10.3 编写审计工具的单元测试
    - 测试完整审计流程
    - 测试选择性检查
    - 测试自动修复功能
    - 测试质量门槛验证

- [x] 11. 实现命令行接口
  - [x] 11.1 创建 CLI 入口和命令定义
    - 创建 `src/cli.ts`
    - 使用 commander 定义命令（audit、migrate-eslint、organize-docs）
    - 实现命令行参数解析
    - 实现错误处理和退出码管理
    - _需求：6.3, 6.4, 6.6, 7.1_

  - [x] 11.2 添加 package.json 配置
    - 配置 bin 字段，指向 CLI 入口
    - 配置 scripts（build、test、lint）
    - 添加所有必要依赖
    - _需求：6.7_

  - [x] 11.3 编写 CLI 的集成测试
    - 测试各个命令的执行
    - 测试参数解析
    - 测试错误处理

- [x] 12. 集成和端到端测试
  - [x] 12.1 创建端到端测试场景
    - 创建测试项目结构
    - 测试完整的审计流程
    - 测试 ESLint 配置迁移
    - 测试文档整理
    - _需求：6.1, 6.2_

  - [x] 12.2 编写 CI/CD 集成测试
    - 测试 CI 模式输出
    - 测试 JUnit XML 报告生成
    - 测试质量门槛验证
    - _需求：10.1, 10.2, 10.3, 10.4, 10.5_

- [x] 13. 文档和示例
  - [x] 13.1 创建使用文档
    - 编写 README.md，包含安装和使用说明
    - 创建使用示例
    - 编写 GitHub Actions 工作流示例
    - _需求：10.6_

  - [x] 13.2 创建 API 文档
    - 为所有公共 API 添加 JSDoc 注释
    - 生成 API 文档（可选）
    - _需求：9.2_

- [x] 14. 最终检查点 - 确保所有测试通过
  - 运行完整的测试套件
  - 验证代码覆盖率达到 80%
  - 验证所有属性测试通过（100 次迭代）
  - 如有问题请询问用户

## 注意事项

- 标记 `*` 的任务为可选测试任务，可以跳过以加快 MVP 开发
- 每个任务都引用了具体的需求编号，确保可追溯性
- 检查点任务确保增量验证，及时发现问题
- 属性测试使用 fast-check，每个测试至少运行 100 次迭代
- 所有文件操作必须支持 dry-run 模式和备份机制
- 错误处理必须提供清晰的错误信息和恢复建议
