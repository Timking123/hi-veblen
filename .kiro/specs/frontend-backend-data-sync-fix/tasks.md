# 实施计划：前后端数据同步修复

## 概述

本实施计划将设计文档中的技术方案转化为可执行的编码任务。任务按照依赖关系组织，确保每个步骤都能在前一步的基础上构建。重点关注核心功能实现，测试任务标记为可选以加快 MVP 交付�?

## 任务列表

### 1. 创建文件同步服务基础设施

创建 `FileSyncService`（文件同步服务）类，提供文件在后台目录和公共目录之间同步的核心功能�?

- 创建 `src/admin/backend/src/services/fileSync.ts` 文件
- 实现 `syncActiveResumeToPublic()` 方法（使用临时文�?+ 原子重命名）
- 实现 `syncAudioToPublic(audioPath)` 方法（支持符号链接和文件复制�?
- 实现 `cleanupPublicDirectory()` 方法（清理过期文件）
- 添加文件同步日志记录�?`file_sync_log` �?
- _需�? 1.1, 1.3, 4.1, 4.2, 4.5_

- [ ] 1.1 编写属性测试：验证简历同步后公共目录文件与源文件内容一�?
  - **属�?2: 激活简历同步�?*
  - **验证: 需�?1.2, 1.3, 1.5, 4.1, 4.2**

- [x] 1.2 编写单元测试：测试临时文件清理和错误处理
  - 测试同步失败时临时文件被正确清理
  - 测试并发同步请求的处�?
  - _需�? 4.5_

### 2. 实现统一下载统计服务

创建 `DownloadStatsService`（下载统计服务）类，统一管理简历下载统计，确保 `resume_versions` 表和 `statistics` 表的数据一致性�?

- 创建 `src/admin/backend/src/services/downloadStats.ts` 文件
- 实现 `recordResumeDownload(version, source)` 方法（事务性更新两个表�?
- 实现 `getTotalResumeDownloads()` 方法
- 实现 `getVersionDownloads(version)` 方法
- 添加数据一致性检查函�?`checkStatsConsistency()`
- _需�? 2.1, 2.2, 2.3, 2.4, 2.5_

- [ ] 2.1 编写属性测试：验证下载统计的一致性和原子�?
  - **属�?3: 下载统计一致�?*
  - **验证: 需�?2.1, 2.2, 2.3, 2.5**

- [ ] 2.2 编写并发测试：验证多个并发下载请求的统计准确�?
  - 使用 100 个并发请求测�?
  - 验证最终计数等于请求数
  - _需�? 2.5_

### 3. 实现文件名编码处理器

创建 `FileEncodingHandler`（文件编码处理器）类，处理文件名�?UTF-8 编码�?RFC 5987 标准编码�?

- 创建 `src/admin/backend/src/utils/fileEncoding.ts` 文件
- 实现 `encodeContentDisposition(filename)` 方法（RFC 5987 + ASCII fallback�?
- 实现 `validateFilename(filename)` 方法（检查非法字符）
- 实现 `normalizeFilename(filename)` 方法（清理特殊字符）
- 添加 UTF-8 验证函数 `isValidUtf8(str)`
- _需�? 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 3.1 编写属性测试：验证文件名编码往返一致�?
  - **属�?4: 文件名编码往返一致�?*
  - **验证: 需�?3.1, 3.2, 3.3, 3.4, 3.5**

- [ ] 3.2 编写单元测试：测试各种特殊字符和多语言文件�?
  - 测试中文、日文、韩文文件名
  - 测试特殊字符的清�?
  - _需�? 3.4_

### 4. 实现缓存控制管理�?

创建 `CacheControlManager`（缓存控制管理器）类，为不同类型的文件设置合适的缓存策略�?

- 创建 `src/admin/backend/src/utils/cacheControl.ts` 文件
- 实现 `getCacheHeaders(fileType, isVersioned)` 方法
- 实现 `generateVersionedUrl(basePath, version)` 方法
- 定义缓存策略常量（当前简�?no-cache，历史版本永久缓存等�?
- _需�? 4.3, 4.4, 10.1, 10.2, 10.3, 10.4, 10.5_

- [ ] 4.1 编写属性测试：验证缓存控制头的正确�?
  - **属�?6: 缓存控制正确�?*
  - **验证: 需�?4.3, 10.1, 10.2, 10.4**

- [ ] 4.2 编写单元测试：测试不同文件类型的缓存策略
  - 测试当前简历返�?no-cache
  - 测试历史版本返回长期缓存
  - _需�? 10.1, 10.2, 10.4_

### 5. 实现文件操作事务管理�?

创建 `FileTransactionManager`（文件事务管理器）类，确保文件操作和数据库操作的原子性�?

- 创建 `src/admin/backend/src/utils/fileTransaction.ts` 文件
- 实现 `executeTransaction(operations, dbOperations)` 方法
- 实现 `rollback(operations)` 方法（回滚文件操作）
- 实现文件备份和恢复机�?
- 添加事务日志记录
- _需�? 4.5, 8.1, 8.2, 8.3_

- [ ] 5.1 编写属性测试：验证文件同步的原子�?
  - **属�?5: 文件同步原子�?*
  - **验证: 需�?4.5, 8.3**

- [ ] 5.2 编写单元测试：测试事务回滚机�?
  - 模拟文件操作失败
  - 验证数据库操作被回滚
  - _需�? 8.3_

### 6. 创建路径映射配置模块

创建集中的路径配置模块，管理所有文件路径映射关系�?

- 创建 `src/admin/backend/src/config/pathMapping.ts` 文件
- 定义 `PathMappingConfig` 接口和实�?
- 实现 `getPublicUrl(adminPath)` 方法
- 实现 `getAdminPath(publicUrl)` 方法
- 导出路径常量（`ADMIN_FILE_ROOT`、`PUBLIC_ROOT` 等）
- _需�? 5.4, 14.1, 14.2, 14.3, 14.4_

- [ ] 6.1 编写单元测试：测试路径映射的正确�?
  - 测试各种文件类型的路径转�?
  - 测试边界情况
  - _需�? 5.4, 14.4_

### 7. 更新简历上传功�?

修改现有的简历上传功能，集成文件同步服务�?

- 修改 `src/admin/backend/src/services/file.ts` 中的 `uploadResume()` 方法
- 上传成功后调�?`FileSyncService.syncActiveResumeToPublic()`（如果是第一个版本）
- 添加文件名编码验�?
- 添加更详细的错误日志
- _需�? 1.1, 3.1, 9.1, 9.2, 9.3_

- [ ] 7.1 编写属性测试：验证上传后文件路径一致�?
  - **属�?1: 简历文件路径一致�?*
  - **验证: 需�?1.1, 1.4**

### 8. 更新简历激活功�?

修改设置当前简历的功能，确保文件同步和数据库更新的原子性�?

- 修改 `src/admin/backend/src/services/file.ts` 中的 `setActiveResume()` 方法
- 使用 `FileTransactionManager` 包装操作
- 调用 `FileSyncService.syncActiveResumeToPublic()` 同步文件
- 添加事务日志记录
- _需�? 1.3, 4.1, 4.2, 4.5_

- [ ] 8.1 编写集成测试：测试完整的激�?同步流程
  - 上传多个版本
  - 切换激活版�?
  - 验证公共目录文件更新
  - _需�? 1.3, 4.2_

### 9. 更新简历下载接�?

修改简历下载接口，集成下载统计服务和缓存控制�?

- 修改 `src/admin/backend/src/controllers/file.ts` 中的 `downloadResumeHandler()`
- 调用 `DownloadStatsService.recordResumeDownload()` 记录统计
- 使用 `FileEncodingHandler.encodeContentDisposition()` 设置响应�?
- 使用 `CacheControlManager.getCacheHeaders()` 设置缓存�?
- 添加 Range 请求支持（断点续传）
- _需�? 2.1, 2.2, 3.2, 4.3, 10.1, 13.3, 13.4_

- [ ] 9.1 编写属性测试：验证下载响应头的正确�?
  - **属�?19: 下载响应头正确�?*
  - **验证: 需�?13.3**

- [ ] 9.2 编写单元测试：测�?Range 请求支持
  - 测试部分内容下载
  - 测试断点续传
  - _需�? 13.4_

### 10. 更新前端简历下载功�?

修改前端的简历下载功能，添加时间戳参数防止缓存�?

- 修改 `src/admin/frontend/src/api/file.ts` 中的 `downloadResume()` 方法（已有时间戳�?
- 修改 `src/views/Contact.vue` 中的 `downloadResume()` 方法
- 添加时间戳查询参�?`?t=${Date.now()}`
- 更新简历路径从 `/file/CV/` 改为 API 端点
- 添加下载错误处理和用户提�?
- _需�? 13.1, 13.2, 13.5_

- [ ] 10.1 编写 E2E 测试：测试前端下载最新简�?
  - 在后台上传新简�?
  - 在前端下�?
  - 验证下载的是最新版�?
  - _需�? 13.2_

### 11. 实现音频文件同步

更新音频文件上传功能，自动同步到公共目录�?

- 修改 `src/admin/backend/src/services/file.ts` 中的 `uploadAudio()` 方法
- 上传成功后调�?`FileSyncService.syncAudioToPublic()`
- 确保音频文件保存到正确的分类目录（BGM/SFX�?
- 添加 Magic Bytes 验证
- _需�? 5.1, 5.2, 5.3, 5.5, 9.1_

- [ ] 11.1 编写属性测试：验证音频文件路径映射正确�?
  - **属�?8: 音频文件路径映射正确�?*
  - **验证: 需�?5.1, 5.2, 5.3**

### 12. 实现留言附件管理

更新留言附件的存储和删除逻辑，确保数据一致性�?

- 修改留言服务中的附件上传功能
- 实现附件元数据记录（文件名、大小、类型）
- 实现留言删除时的级联删除附件
- 添加附件访问权限控制
- _需�? 6.1, 6.2, 6.3, 6.4, 6.5_

- [ ] 12.1 编写属性测试：验证留言附件级联删除
  - **属�?9: 留言附件级联删除**
  - **验证: 需�?6.5, 8.2**

### 13. 实现文件访问权限控制

更新路由配置，实施文件访问权限控制�?

- 修改 `src/admin/backend/src/routes/file.ts` 路由配置
- 为公共文件路由（简历、音频）移除认证中间�?
- 为私有文件路由（留言附件）添加认证中间件
- 实现路径遍历防护中间�?
- 添加文件访问日志记录
- _需�? 7.1, 7.2, 7.3, 7.4, 7.5_

- [ ] 13.1 编写属性测试：验证文件访问权限正确�?
  - **属�?10: 文件访问权限正确�?*
  - **验证: 需�?7.1, 7.2**

- [ ] 13.2 编写安全测试：测试路径遍历防�?
  - **属�?11: 路径遍历防护**
  - **验证: 需�?7.4**

### 14. 增强文件上传验证

更新文件上传验证逻辑，实施双重验证（扩展�?+ Magic Bytes）�?

- 修改 `src/admin/backend/src/services/file.ts` 中的验证函数
- 实现 `validateUploadedFile()` 统一验证函数
- 同时检查文件扩展名�?Magic Bytes
- 添加文件大小验证（简�?20MB、图�?10MB、音�?50MB�?
- 拒绝零字节文�?
- 添加安全警告日志
- _需�? 9.1, 9.2, 9.3, 9.4, 9.5_

- [ ] 14.1 编写属性测试：验证文件上传双重验证
  - **属�?13: 文件上传双重验证**
  - **验证: 需�?9.1, 9.2, 5.5**

- [ ] 14.2 编写属性测试：验证文件大小限制
  - **属�?14: 文件大小限制**
  - **验证: 需�?9.3, 9.4**

- [ ] 14.3 编写属性测试：验证文件名安全�?
  - **属�?15: 文件名安全�?*
  - **验证: 需�?9.5**

### 15. 实现错误处理和日志记�?

增强错误处理和日志记录功能�?

- 创建 `src/admin/backend/src/utils/errorHandler.ts` 文件
- 实现统一的错误处理中间件
- 为不同类型的错误定义错误类（`FileOperationError`、`EncodingError` 等）
- 增强日志记录，包含详细的上下文信�?
- 实现日志查询接口
- _需�? 11.1, 11.2, 11.3, 11.4, 11.5_

- [ ] 15.1 编写属性测试：验证错误日志完整�?
  - **属�?17: 错误日志完整�?*
  - **验证: 需�?11.1, 11.2, 11.3**

### 16. 实现文件元数据管�?

增强文件元数据的记录和查询功能�?

- 为文件记录添加更多元数据字段（上传者、MIME 类型等）
- 实现文件历史记录功能
- 实现元数据查询接�?
- 添加元数据验�?
- _需�? 12.1, 12.2, 12.3, 12.4, 12.5_

- [ ] 16.1 编写属性测试：验证文件元数据完整�?
  - **属�?18: 文件元数据完整�?*
  - **验证: 需�?12.1, 12.4**

### 17. 创建数据库迁移脚�?

创建数据库表结构更新脚本�?

- 创建 `src/admin/backend/src/database/migrations/` 目录
- 创建 `file_sync_log` 表的迁移脚本
- 创建 `download_log` 表的迁移脚本（可选）
- 实现迁移执行和回滚功�?
- _需�? 8.4, 15.1_

### 18. 创建数据一致性检查工�?

创建命令行工具检查和修复数据不一致�?

- 创建 `src/admin/backend/src/scripts/checkConsistency.ts` 脚本
- 实现检查数据库记录与文件系统的一致�?
- 实现检查激活简历的公共副本
- 实现检查孤立文�?
- 生成详细的检查报�?
- _需�? 8.4, 15.1_

### 19. 创建文件同步工具

创建命令行工具同步所有文件到公共目录�?

- 创建 `src/admin/backend/src/scripts/syncFiles.ts` 脚本
- 实现同步当前简�?
- 实现同步所有音频文�?
- 生成同步报告
- 添加备份功能
- _需�? 15.2_

### 20. 创建编码修复工具

创建命令行工具修复文件名编码问题�?

- 创建 `src/admin/backend/src/scripts/fixEncoding.ts` 脚本
- 实现检测和转换文件名编�?
- 实现批量更新数据库记�?
- 生成修复报告
- 添加备份功能
- _需�? 15.3_

### 21. 更新配置文件

更新项目配置文件，集中管理路径配置�?

- 创建 `src/admin/backend/src/config/files.ts` 配置文件
- 定义所有文件路径常�?
- 定义文件大小限制常量
- 定义缓存策略常量
- 更新环境变量配置
- _需�? 14.4_

### 22. 编写迁移文档

编写数据迁移和部署文档�?

- 创建 `docs/MIGRATION_GUIDE.md` 文档
- 记录迁移步骤和注意事�?
- 记录回滚方案
- 记录常见问题和解决方�?
- _需�? 15.4, 15.5_

### 23. 执行数据迁移

执行实际的数据迁移操作�?

- 创建完整备份
- 运行一致性检查脚�?
- 运行文件同步脚本
- 运行编码修复脚本
- 验证迁移结果
- _需�? 15.1, 15.2, 15.3, 15.4_

### 24. 最终验证和测试

运行完整的测试套件验证所有功能�?

- 运行所有单元测�?
- 运行所有属性测�?
- 运行所有集成测�?
- 运行 E2E 测试
- 手动测试关键功能
- 验证数据一致�?
- _需�? 所有需求_

## 注意事项

- 标记�?`*` 的任务是可选的测试任务，可以跳过以加快 MVP 交付
- 每个任务都引用了相关的需求编号，便于追溯
- 属性测试应该使�?fast-check 库，每个测试至少运行 100 次迭�?
- 所有文件操作都应该有详细的错误处理和日志记�?
- 在修改现有代码前，确保理解当前的实现逻辑
- 数据迁移前务必创建完整备�?
