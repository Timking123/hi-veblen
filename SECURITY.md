# 安全策略

## 支持范围

安全修复以 `main` 最新代码和当前生产 revision 为准。历史规划文档、未部署分支和本地实验不提供单独版本支持。

## 私密报告漏洞

请通过 [GitHub Private Security Advisory](https://github.com/Timking123/hi-veblen/security/advisories/new) 私下报告问题。涉及 Lingxi 角色、会话或对话数据时仍使用这个公开可达的报告入口，并在标题中标明 `Lingxi`；Lingxi 私有仓库的 Advisory 不能作为外部报告入口。

不要在公开 Issue、Discussion、提交信息或截图中披露可利用细节、凭据、非公开基础设施地址、用户数据或完整请求记录。`hi-veblen.com` 与 `lingxi.hi-veblen.com` 是公开产品地址，不属于这一限制。

报告中请尽量包含：

- 受影响的 URL、组件与 revision。
- 最小复现步骤和实际影响。
- 已验证的前置条件与权限范围。
- 建议修复或临时缓解方式。
- 已做的数据清理，确保附件不含真实用户内容和密钥。

维护者会在 Private Security Advisory 中确认接收、评估影响并协调修复与披露。当前项目不承诺固定响应时限，也不接受以生产破坏或真实数据访问为前提的测试。

## 测试边界

允许在自有本地环境中进行静态分析、依赖审计和最小化复现。未经明确授权，不要执行以下操作：

- 扫描、压测或利用生产服务。
- 尝试获取其他用户的会话、角色、消息、上传文件或管理数据。
- 绕过认证、持久化恶意内容或影响服务可用性。
- 下载、传播或保留意外接触到的生产数据。

## 凭据与运行数据

`.env`、数据库、上传文件、日志与备份属于运行数据，不得提交到 Git。发现疑似泄露时，先停用或轮换对应凭据，再评估 Git 历史、制品、Actions 日志和服务器副本；只删除当前工作树中的文件不能消除历史暴露。
